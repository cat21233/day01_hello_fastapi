# D6 预习卡：重构项目结构 + async 加练 + 费曼输出

> 今天（D5）已打卡，连续 5 天。D6 是阶段一里\*\*第一次"像工程师一样整理代码"\*\*的一天——  
> 前 5 天所有接口都堆在一个 `main.py` 里，D6 把它们拆成模块化结构，并补上配置管理和异步专项练习。  
> 本卡只预习概念和明天要做的题，**不剧透答案**——明天动手时我会让你闭卷写。

---

## 一、D6 三大块（来自学习计划）

| 任务            | 类型         | 优先级 | 你要做的                                  |
| ------------- | ---------- | --- | ------------------------------------- |
| t-037         | coding     | 高   | 重构项目结构（routers / config / middleware） |
| t-038         | weak_focus | 高   | async/await 异步编程专项加练（动手题）             |
| t-040         | output     | 高   | 费曼输出：用自己话讲 FastAPI 基础 + async 入门      |
| t-041         | output     | 高   | 本周技术博客撰写                              |
| t-039 / t-042 | review     | 中   | 艾宾浩斯复现 + 概念笔记整理（D1/D3 节点）             |

---

## 二、你现在 main.py 里有什么（拆之前先盘点）

```
中间件：
  add_process_time  → 记录每个请求耗时，响应头加 X-Process-Time

路由（都在 app = FastAPI() 上）：
  GET  /                         → read_root
  GET  /items/                  → read_items（分页 skip/limit，Depends）
  GET  /items/{item_id}         → read_item（Path 校验）
  POST /items/                  → create_item（请求体 Item）
  POST /users/                  → create_user（请求体 User，含 Address 嵌套）
  GET  /items/{item_id}/public  → read_item_public（response_model=ItemPublic）
  GET  /health                  → health（async def）
  GET  /me                      → read_me（子依赖鉴权 get_token→get_current_user）
  GET  /stream/greet            → stream_greet（SSE 流式）
  GET  /external/users/{user_id}→ get_external_user（httpx 异步调外部 API）
```

**问题**：单文件 160+ 行，模型定义、依赖、路由全混在一起。真实项目这样没法维护。

---

## 三、核心概念预习（理解"为什么"，明天才写得出来）

### 概念 1：APIRouter —— 路由"分家"

**为什么需要**：当一个文件里有 10 个接口 + 一堆模型 + 中间件，改一个接口要在一大坨代码里找。  
真实项目按"业务模块"拆分：`items`（商品）、`users`（用户）、`stream`（流式）、`external`（外部调用）。

**怎么做（明天闭卷写）**：

- 每个模块建一个 `router = APIRouter()`，把 `@app.get(...)` 改成 `@router.get(...)`
- `main.py` 里 `app.include_router(items_router)` 把它们组装回来
- 模型（Item / User / Address）抽到 `schemas.py` 或各 router 内

**关键认知**：`include_routebr` 就是把拆出去的路由"挂回"主 app，功能完全不变，只是文件结构清晰了。

### 概念 2：pydantic-settings —— 配置外置

**为什么需要**：你现在的代码里如果有端口号、密钥、token，都是**硬编码写死**的。  
硬编码的坑：① 换环境要改代码 ② 密钥容易误提交到 GitHub 泄露。

**怎么做**：

- 装包：`uv pip install pydantic-settings`
- 建一个 `Settings` 类继承 `BaseSettings`，字段从 `.env` 文件读取
- 代码里用 `settings.APP_NAME` / `settings.PORT`，不再写死字符串
- `.env` 文件加到 `.gitignore`（密钥不进版本库）

**关键认知**：配置（会变、可能保密）和代码（不变）分离，是工程基本功。

### 概念 3：async 专项加练（明天的动手题方向）

你 D3 学了 `async def`、D4 用了 `await asyncio.sleep`、D5 用了 `await httpx`。  
明天要**亲手验证"异步并发比同步快"**：

- 写两个接口或一个脚本：一个同步顺序请求 3 个外部 API，一个 `asyncio.gather` 并发请求同样 3 个
- 对比总耗时，亲眼看到并发省了多少时间
- 这直接巩固你 D5 学的"await 等待时不卡事件循环"

---

## 四、明天要做的动手题（先预告，今晚不用写）

**题 1（重构）**：把当前 `main.py` 拆成以下结构（闭卷写，我明天验收）：

```
main.py              → 只保留 app 实例 + 中间件 + include_router
routers/items.py     → /items/ 系列 4 个接口 + Item/ItemPublic 模型
routers/users.py     → /users/ + /me + User/Address 模型 + 子依赖鉴权
routers/stream.py    → /stream/greet (SSE)
routers/external.py  → /external/users/{user_id} (httpx)
```

**题 2（配置）**：用 pydantic-settings 建 `Settings`，从 `.env` 读 `APP_NAME` / `PORT`，在 `main.py` 里用它。  
**题 3（async 加练）**：并发 vs 同步请求外部 API 的耗时对比。

---

## 五、今晚 / 明早可以做的准备

1. **想清楚模块边界**：上面的拆分方案里，`items` 和 `users` 的界限你认不认？有没有接口你觉得该归到别处？（先思考，明天直接写）
2. **复习三处旧知识**（明天重构全要用）：
   - D2 的 `Depends` 分页（`common_params`）
   - D3 的 `response_model` + 子依赖鉴权
   - D5 的 `httpx.AsyncClient` 异步
3. **确认 venv 能装新包**：明天要 `uv pip install pydantic-settings`，你的 venv 是 uv 创建的，用 `uv pip install` 而不是 `pip install`。

---

## 六、费曼输出提示（t-040 / t-041）

D6 收尾时要你"用自己话讲 FastAPI 基础 + async 入门"，并写成博客草稿。可以围绕这几个你**已经踩过坑**的点讲：

- `=` 与 `:` 的区别（请求体类型注解）
- `Query` / `Path` / `Field` 怎么区分
- `Optional` 和 `= None` 谁负责什么
- 为什么 web 接口不能用 `input()` 而用 `Query`
- `await` 为什么比 `time.sleep` 好

这些你都真踩过，讲出来最有说服力。

---

## 七、环境坑提醒（避免明天重踩）

- **安装包用 `uv pip install xxx`**，不要 `uv add`（项目没有 pyproject.toml）、不要裸 `pip`（venv 没装 pip）
- **Windows 验证响应头**：用 `/docs` 点 Execute 看 Response headers，或 `curl -i http://127.0.0.1:8000/`，别用 `/dev/null`
- **GitHub push 需开 VPN**：直连会被 RST 重置
- **`--reload` 在沙箱里不生效**：改完代码手动 Ctrl+C 重启

---

明天开 D6 时，我会让你先闭卷写"题 1 重构"，写完我逐文件 review。今晚看一遍这张卡就够了，不用动手。
